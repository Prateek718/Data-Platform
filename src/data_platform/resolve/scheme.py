"""R3-SCHEME-01 — scheme identity resolution (stub; implemented next commit)."""

from __future__ import annotations

CANONICAL_SCHEME = "MGNREGA"


def resolve_scheme(label: str | None) -> str | None:
    """Resolve a scheme label to the canonical scheme id, or ``None`` if unknown."""
    return None
